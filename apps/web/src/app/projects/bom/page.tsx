"use client";

import { FormEvent, useEffect, useMemo, useState } from "react";
import { StatusBadge } from "@/components/network/status-badge";
import { Modal } from "@/components/ui/modal";
import { SearchableSelect } from "@/components/ui/searchable-select";
import { fetchInventoryItems } from "@/hooks/use-inventory";
import { createProjectBom, deleteProjectBom, fetchProjectBoms, fetchProjects, projectBomAction, updateProjectBom, useProjectList } from "@/hooks/use-projects";
import { formatMoney } from "@/lib/format";
import type { InventoryItem } from "@/types/inventory";
import type { Project, ProjectBom, ProjectBomStatus } from "@/types/project";

type Line = { itemId: string; quantity: string; estimatedUnitCost: string; notes: string };
const emptyLine: Line = { itemId: "", quantity: "1", estimatedUnitCost: "0", notes: "" };
const emptyForm = { projectId: "", bomNumber: "", status: "draft" as ProjectBomStatus, notes: "", items: [{ ...emptyLine }] };
const statuses: Array<{ label: string; value: ProjectBomStatus | "" }> = [{ label: "All statuses", value: "" }, { label: "Draft", value: "draft" }, { label: "Approved", value: "approved" }, { label: "Issued", value: "issued" }, { label: "Cancelled", value: "cancelled" }];
function total(bom: ProjectBom) { return bom.items.reduce((sum, item) => sum + Number(item.quantity) * Number(item.estimatedUnitCost), 0); }

export default function ProjectBomPage() {
  const [search, setSearch] = useState(""); const [status, setStatus] = useState<ProjectBomStatus | "">(""); const [page, setPage] = useState(1);
  const [formOpen, setFormOpen] = useState(false); const [editing, setEditing] = useState<ProjectBom | null>(null); const [form, setForm] = useState(emptyForm);
  const [projects, setProjects] = useState<Project[]>([]); const [items, setItems] = useState<InventoryItem[]>([]);
  const [message, setMessage] = useState(""); const [localError, setLocalError] = useState(""); const [saving, setSaving] = useState(false);
  const query = useMemo(() => ({ search, status, page, limit: 10 }), [page, search, status]);
  const { data, loading, error, reload } = useProjectList<ProjectBom>(fetchProjectBoms, query, "Unable to load BOMs");
  const totalPages = Math.max(Math.ceil(data.meta.total / data.meta.limit), 1);
  useEffect(() => { Promise.all([fetchProjects({ limit: 200 }), fetchInventoryItems({ filter: "active", limit: 300 })]).then(([p, i]) => { setProjects(p.items); setItems(i.items); }).catch(() => undefined); }, []);
  function openCreate() { setEditing(null); setForm(emptyForm); setLocalError(""); setFormOpen(true); }
  function openEdit(bom: ProjectBom) { setEditing(bom); setForm({ projectId: String(bom.projectId), bomNumber: bom.bomNumber, status: bom.status, notes: bom.notes ?? "", items: bom.items.map((item) => ({ itemId: String(item.itemId), quantity: String(item.quantity), estimatedUnitCost: String(item.estimatedUnitCost), notes: item.notes ?? "" })) }); setLocalError(""); setFormOpen(true); }
  function updateLine(index: number, patch: Partial<Line>) { setForm((c) => ({ ...c, items: c.items.map((line, i) => i === index ? { ...line, ...patch } : line) })); }
  function payload() { return { projectId: Number(form.projectId), bomNumber: form.bomNumber || null, status: form.status, notes: form.notes || null, items: form.items.map((item) => ({ itemId: Number(item.itemId), quantity: Number(item.quantity), estimatedUnitCost: Number(item.estimatedUnitCost || 0), notes: item.notes || null })) }; }
  async function submit(event: FormEvent) { event.preventDefault(); setSaving(true); setMessage(""); setLocalError(""); try { if (editing) { await updateProjectBom(editing.id, payload()); setMessage("BOM updated."); } else { await createProjectBom(payload()); setMessage("BOM created."); } setFormOpen(false); await reload(); } catch (caught) { setLocalError(caught instanceof Error ? caught.message : "Unable to save BOM"); } finally { setSaving(false); } }
  async function run(action: () => Promise<unknown>, success: string) { setSaving(true); setMessage(""); setLocalError(""); try { await action(); setMessage(success); await reload(); } catch (caught) { setLocalError(caught instanceof Error ? caught.message : "Unable to update BOM"); } finally { setSaving(false); } }
  return <section className="flex flex-col gap-5"><header className="flex flex-col gap-3 md:flex-row md:items-end md:justify-between"><div><p className="text-xs font-semibold uppercase tracking-wide text-emerald-700">Projects</p><h1 className="text-2xl font-semibold text-slate-950">Bill of Materials</h1><p className="text-sm text-slate-600">Plan required inventory materials for each project.</p></div><button onClick={openCreate} className="rounded-md bg-emerald-600 px-4 py-2 text-sm font-semibold text-white">Create BOM</button></header>
    <div className="rounded-md border bg-white shadow-sm"><div className="flex flex-col gap-3 border-b p-4 lg:flex-row"><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search BOM or project" className="w-full rounded-md border px-3 py-2 text-sm" /><SearchableSelect value={status} onChange={(nextValue) => { setStatus(nextValue as ProjectBomStatus | ""); setPage(1); }} includeEmptyOption={false} options={statuses} className="rounded-md border px-3 py-2 text-sm" /><button onClick={() => { setPage(1); reload(); }} className="rounded-md border border-emerald-600 px-3 py-2 text-sm text-emerald-700">Search</button></div>{message ? <div className="border-b bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{message}</div> : null}{(localError || error) ? <div className="border-b bg-red-50 px-4 py-3 text-sm text-red-700">{localError || error}</div> : null}<div className="overflow-x-auto"><table className="w-full min-w-[1000px] text-left text-sm"><thead className="bg-emerald-950 text-white"><tr><th className="px-4 py-3">BOM</th><th className="px-4 py-3">Project</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Lines</th><th className="px-4 py-3">Estimate</th><th className="px-4 py-3">Actions</th></tr></thead><tbody>{data.items.map((bom) => <tr key={bom.id} className="border-b"><td className="px-4 py-3 font-medium">{bom.bomNumber}</td><td className="px-4 py-3">{bom.project.code} - {bom.project.name}</td><td className="px-4 py-3"><StatusBadge status={bom.status} /></td><td className="px-4 py-3">{bom.items.length}</td><td className="px-4 py-3">{formatMoney(total(bom))}</td><td className="px-4 py-3"><div className="flex flex-wrap gap-2">{bom.status === "draft" ? <button onClick={() => run(() => projectBomAction(bom.id, "approve"), "BOM approved.")} className="rounded-md border border-emerald-600 px-2.5 py-1.5 text-xs text-emerald-700">Approve</button> : null}{bom.status === "approved" ? <button onClick={() => run(() => projectBomAction(bom.id, "issue"), "BOM issued.")} className="rounded-md border border-sky-600 px-2.5 py-1.5 text-xs text-sky-700">Issue</button> : null}<button onClick={() => openEdit(bom)} className="rounded-md border px-2.5 py-1.5 text-xs">Edit</button><button onClick={() => run(() => deleteProjectBom(bom.id), "BOM deleted.")} className="rounded-md border border-red-200 px-2.5 py-1.5 text-xs text-red-700">Delete</button></div></td></tr>)}{!data.items.length ? <tr><td colSpan={6} className="px-4 py-8 text-center text-slate-500">{loading ? "Loading..." : "No BOMs found."}</td></tr> : null}</tbody></table></div><div className="flex items-center justify-between border-t px-4 py-3 text-sm text-slate-600"><span>Page {data.meta.page} of {totalPages} - {data.meta.total} records</span><div className="flex gap-2"><button disabled={page <= 1} onClick={() => setPage((p) => p - 1)} className="rounded-md border px-3 py-1.5 disabled:opacity-40">Previous</button><button disabled={page >= totalPages} onClick={() => setPage((p) => p + 1)} className="rounded-md border px-3 py-1.5 disabled:opacity-40">Next</button></div></div></div>
    <Modal open={formOpen} title={editing ? "Edit BOM" : "Create BOM"} onClose={() => setFormOpen(false)}><form onSubmit={submit} className="grid gap-4"><div className="grid gap-4 md:grid-cols-2"><label className="flex flex-col gap-1 text-sm font-medium">Project<SearchableSelect required value={form.projectId} onChange={(nextValue) => setForm((c) => ({ ...c, projectId: nextValue }))} emptyOptionLabel="Select project" options={projects.map((p) => ({ label: `${p.code} - ${p.name}`, value: String(p.id) }))} className="rounded-md border px-3 py-2" /></label><label className="flex flex-col gap-1 text-sm font-medium">BOM No.<input value={form.bomNumber} onChange={(e) => setForm((c) => ({ ...c, bomNumber: e.target.value }))} placeholder="Auto if blank" className="rounded-md border px-3 py-2" /></label><label className="flex flex-col gap-1 text-sm font-medium">Status<SearchableSelect value={form.status} onChange={(nextValue) => setForm((c) => ({ ...c, status: nextValue as ProjectBomStatus }))} includeEmptyOption={false} options={statuses.filter((s) => s.value)} className="rounded-md border px-3 py-2" /></label></div><div className="rounded-md border"><div className="flex items-center justify-between border-b px-3 py-2"><h3 className="text-sm font-semibold">Materials</h3><button type="button" onClick={() => setForm((c) => ({ ...c, items: [...c.items, { ...emptyLine }] }))} className="rounded-md border px-2.5 py-1 text-xs">Add Line</button></div><div className="grid gap-3 p-3">{form.items.map((line, index) => <div key={index} className="grid gap-2 rounded-md border p-3 md:grid-cols-5"><SearchableSelect required value={line.itemId} onChange={(nextValue) => { const selected = items.find((item) => String(item.id) === nextValue); updateLine(index, { itemId: nextValue, estimatedUnitCost: selected ? String(selected.unitCost) : line.estimatedUnitCost }); }} emptyOptionLabel="Select item" options={items.map((item) => ({ label: `${item.code} - ${item.name}`, value: String(item.id) }))} className="rounded-md border px-2 py-2 text-sm md:col-span-2" /><input required type="number" min="0.01" step="0.01" value={line.quantity} onChange={(e) => updateLine(index, { quantity: e.target.value })} className="rounded-md border px-2 py-2 text-sm" /><input type="number" min="0" step="0.01" value={line.estimatedUnitCost} onChange={(e) => updateLine(index, { estimatedUnitCost: e.target.value })} className="rounded-md border px-2 py-2 text-sm" /><button type="button" disabled={form.items.length <= 1} onClick={() => setForm((c) => ({ ...c, items: c.items.filter((_, i) => i !== index) }))} className="rounded-md border border-red-200 px-2 py-2 text-xs text-red-700 disabled:opacity-40">Remove</button></div>)}</div></div><label className="flex flex-col gap-1 text-sm font-medium">Notes<textarea value={form.notes} onChange={(e) => setForm((c) => ({ ...c, notes: e.target.value }))} rows={2} className="rounded-md border px-3 py-2" /></label><div className="flex justify-end gap-2 border-t pt-4"><button type="button" onClick={() => setFormOpen(false)} className="rounded-md border px-4 py-2 text-sm">Cancel</button><button disabled={saving} className="rounded-md bg-emerald-600 px-4 py-2 text-sm font-semibold text-white">{saving ? "Saving..." : "Save BOM"}</button></div></form></Modal>
  </section>;
}
