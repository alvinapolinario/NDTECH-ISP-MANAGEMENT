# Database Package

This package is reserved for shared database assets such as Prisma schema, migrations, and seeders.

Current Phase 1 note: the generated NestJS app currently keeps Prisma under `apps/api/prisma`. Keep future database changes aligned before moving schema files here.
